# Copy this file to .streamlit/secrets.toml for local development,
# or paste its contents into "Secrets" in Streamlit Community Cloud's app settings.
# Both are optional — you can also just paste a key into the sidebar each session.

GROQ_API_KEY = "gsk_..."
GEMINI_API_KEY = "AIza..."
